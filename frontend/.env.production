# Production build for Netlify → local Django via HTTPS tunnel (avoids mixed content).
# Tunnel must be running while you use the Netlify site:
#   npx -y localtunnel --port 8000
# Then update this URL if localtunnel prints a new one.
NEXT_PUBLIC_API_ORIGIN=https://floppy-towns-invite.loca.lt
